<launch>
  <!-- Arguments -->
  <arg name="slam_methods" default="gmapping" doc="slam type [gmapping, cartographer, hector, karto, frontier_exploration]"/>
  <arg name="configuration_basename" default="cora_lds_2d.lua"/>
  <arg name="open_rviz" default="true"/>

  <!-- Gmapping -->
  <node pkg="gmapping" type="slam_gmapping" name="cora_slam_gmapping" output="screen">
    <param name="base_frame" value="base_footprint"/>
    <param name="odom_frame" value="world"/>
    <param name="map_frame"  value="map"/>
    <rosparam command="load" file="$(find ay22_gdk)/config/gmapping_params.yaml" />
  </node>

  <!-- rviz -->
  <group if="$(arg open_rviz)"> 
    <node pkg="rviz" type="rviz" name="rviz" required="true"
          args="-d $(find ay22_gdk)/config/cora1.rviz"/>
  </group>
  
  <node pkg="pointcloud_to_laserscan" type="pointcloud_to_laserscan_node" name="pointcloud_to_laserscan" required="true">
    <remap from="cloud_in" to="/cora1/cora/sensors/lidars/lidar/points"/>
  </node>
    
</launch>
